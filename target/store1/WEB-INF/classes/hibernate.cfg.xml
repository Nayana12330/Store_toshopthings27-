<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>

        <!-- DB CONNECTION -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>

        <property name="hibernate.connection.url">
            jdbc:mysql://localhost:3306/store?useSSL=false&amp;serverTimezone=UTC
        </property>

        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">Nayana@2701#</property>

        <!-- DIALECT -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- DEBUG -->
        <property name="hibernate.show_sql">true</property>

        <!-- AUTO UPDATE -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ENTITY MAPPINGS -->
        <mapping class="com.store1.model.entity.User"/>
        <mapping class="com.store1.model.entity.Product"/>

    </session-factory>
</hibernate-configuration>